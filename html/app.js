/**
 * app.js
 * -------
 *
 * A test app for formdata work.
 *
 */

document.addEventListener( "alpine:init", () => {

	Alpine.data( "app", () => ({
		show() {
			//console.log( $formdata );
		}
	}))

})
