
# Minify these resources (there should be a shell equivalent for this)
# Could PROBABLY use the cpp to get the job done
minify:


# Convert from markdown into HTML for quick testing
docstest:
	markdown README.md > readme.html
